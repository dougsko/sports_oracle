<%= form_for(@stat_entry) do |f| %>
    <% if @stat_entry.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(@stat_entry.errors.count, "error") %> prohibited this stat_entry from being saved:</h2>

            <ul>
                <% @stat_entry.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <div class="field">
        <% f.label :stat %>
        <%= f.collection_select :stat_id, @stats, :id, :name %>
    </div>
    <div class="field">
        <%= f.label :year %><br>
        <%= f.text_field :year %>
    </div>
    <div class="field">
        <%= f.label :week %><br>
        <%= f.number_field :week %>
    </div>
    <div class="field">
        <%= f.label :team %><br>
        <%= f.collection_select :team_id, @teams, :id, :name %>
    </div>
    <div class="field">
        <%= f.label :value %><br>
        <%= f.text_field :value %>
    </div>
    <div class="actions">
        <%= f.submit %>
    </div>
<% end %>
